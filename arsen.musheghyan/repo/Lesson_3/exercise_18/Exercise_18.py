#!/usr/bin/env python3
import math
radius = int(input("Enter length number of radius: "))
print ("The circle's diameter is:", radius * 2)
print ("The circle's length is:  ", '%0.05f' % (math.pi * radius * 2))
print ("The circle's area is:    ", '%0.05f' % (math.pi * (radius ** 2)))